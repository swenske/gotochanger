# Loaded automatically at boot by systemd-modules-load once the
# gotochanger-kernel package is installed - so an admin enabling kernel
# mode (gotochanger-tcmud@<name>.service) doesn't need to modprobe these
# by hand first. See CLAUDE.md's "Kernel mode (TCMU/LIO)" section.
target_core_mod
target_core_user
tcm_loop
